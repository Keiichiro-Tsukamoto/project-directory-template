# 運用詳細ガイド

## 目的

この文書は、LLM向けの読み込み順、タスクと成果物のライフサイクル、context登録基準、検証方法をまとめます。この文書が必要な場合は、対象タスクの`context.md`へ登録します。登録されていない通常のタスクでは読みません。内容が`_control/rules.md`と矛盾する場合は、`rules.md`を優先します。

## 管理ファイル

- `_control/project.md`：プロジェクトの背景、目的、達成目標
- `_control/tasks.md`：タスク一覧、ステータス、task detailの場所
- `_control/context.md`：Task IDと、そのタスクで読むファイルの対応
- `_control/rules.md`：常に優先する短い運用規則

管理ファイルには履歴や長い説明を置かず、詳細は必要時だけ読むreference文書へ分離します。

## タスク開始時の読み込み順

1. `_control/project.md`
2. `_control/tasks.md`
3. `_control/context.md`
4. activeタスクのtask detail file
5. active Task IDとして`context.md`へ登録されたファイル

`current/`、`reference/`、既存ソースコードを一括で読みません。Goalに必要な入力が不足している場合は、プロジェクト全体を探索せず人へ確認します。

## タスクのライフサイクル

使用するステータスは`pending`、`active`、`blocked`、`done`の4つで、activeは最大1件です。

activeは、作業中、承認待ち、修正中、再レビュー中をすべて含みます。レビューの往復を別ステータスとして記録しません。

```text
タスク開始
  ↓
入力資料をcontextへ登録
  ↓
wipで作業・レビュー・修正
  ↓
人が成果物確定を承認
  ↓
継続成果物をcurrent、旧版と完了資料をarchiveへ移動
  ↓
タスクをdoneにし、contextを後続タスク用に整理
```

成果物の確定、`current/`への移動、タスクの`done`への変更には、人の明示的な承認が必要です。

## context登録基準

次をactive Task IDの行として登録します。

- タスク開始時の入力資料
- 継続、レビュー、修正、再現に必要な一次成果物
- 再現または検収に必要なスクリプト、設定、エビデンス
- 過去タスクの成果物を再利用する場合の現行ファイル
- 外部リソースを使う場合の参照記述と、必要なら登録済みスナップショット

次は登録しません。

- 一時ファイル、キャッシュ、実行ログ
- 重複形式
- 自動再生成でき、検収や再現に不要な中間生成物
- 外部URLそのもの
- タスクに関係しないファイル

成果物を作成または移動したら、レビューを依頼する前にcontextのパスが存在することを確認します。

## 成果物の配置

- `wip/`：新規、未確定、レビュー中の成果物
- `current/`：承認済みの現在有効な成果物
- `reference/`：背景資料、外部資料、必要時だけ読む詳細文書
- `archive/`：旧版、完了済み、置換済み、廃止済みの資料

複数ファイルの成果物は原則として`wip/T-XXX/`へまとめます。一時物は`wip/T-XXX/.tmp/`へ置き、Gitとcontextの対象外にします。

現行成果物を修正する場合は、`current/`を直接変更せず、新しいタスクの入力として登録して修正版を`wip/`へ作ります。承認後に旧版を`archive/`、修正版を`current/`へ移します。

## 人による承認

次はモデル階層にかかわらず、人または既存プロジェクト方針による明示的な承認を必要とします。

- WIP成果物の確定と`current/`への昇格
- 外部リソースの追加、更新、削除、共有、移動
- 許可が必要な外部内容のローカル保存またはGit登録
- `.gitignore`変更、branch、stage、commit、push
- 正とする成果物や方針を決める必要がある矛盾

## モデル分業

作業は、決定的ツール、低コストモデル、標準推論モデル、上位モデルの順に、必要条件を満たす最小コストの方法を検討します。

別モデルまたはエージェントへ委譲する場合は、次を明示します。

```text
Task ID: T-XXX
Subtask: 1つの具体的な作業
Goal: 合格状態
Allowed inputs:
- contextへ登録済みの正確なパス
Allowed output:
- 専用WIPパス
Do not:
- 禁止操作
Validation:
- テストまたはチェック項目
Escalate when:
- 判断不能または失敗条件
```

並列処理は、activeタスク内部の独立した下位作業だけに使います。同じファイルを同時編集せず、各作業へ別の出力先を指定します。

## 外部リソース

外部リソースは`reference/external/`の参照記述を介してcontextへ登録します。参照記述に指定されたリソースと取得範囲だけを読みます。

取得リビジョンやエラーなどの一時状態は`wip/T-XXX/.tmp/external-state/`へ置きます。検収または再現に必要な記録だけをWIPエビデンスへ昇格させてcontextへ登録します。

外部アクセスやリビジョン検証に失敗した場合は、Web検索、類似文書、未登録キャッシュへ暗黙に置き換えません。リモート正式版とローカル承認版が異なる場合も、正とする版を推測せず人へ確認します。

## 変更検出と検証

Gitを利用できる場合は、現在タスクの管理ファイル、task detail、context指定ファイル、専用成果物に対象を限定して`status`と`diff`を確認します。

Gitを利用できない場合や、Gitと独立して確認する場合はハッシュスナップショットを使います。

```text
python3 current/context-tools/context_snapshot.py snapshot
python3 current/context-tools/context_snapshot.py check
```

構造と参照の静的検証には次を使います。

```text
python3 current/context-tools/validate_template.py . --output wip/T-XXX/validation.json
```

静的検証で確認できるのは、Task ID、ステータス、active件数、detail、context参照、必須ディレクトリ、登録された外部参照記述などです。次は人または実行時の確認が必要です。

- Goalに対して意味的に必要な入力が揃っているか
- 成果物の内容が要求を満たすか
- 外部サービスへ実際にアクセスできるか
- リモートリビジョンと権限が正しいか
- 検出された変更の理由が妥当か

## エスカレーション

次の場合は推測で続けず、上位モデルまたは人へ判断を戻します。

- 必要な入力が登録されていない
- 指示、入力、`rules.md`の間に矛盾がある
- 合否条件を満たせない、または検証できない
- 作業範囲外のファイル変更が必要になる
- 秘密情報、外部書き込み、破壊的操作、Git変更操作が必要になる
- 正しさに影響する不確実性が残る

## タスク完了時の確認

1. 人による成果物の承認がある
2. 継続成果物が`current/`へ移動されている
3. 旧版と完了資料が`archive/`へ移動されている
4. task detailが`archive/`へ移動されている
5. `tasks.md`が`done`へ更新されている
6. 完了タスクの不要なcontext行が削除されている
7. 後続タスクの参照先が移動後のパスへ更新されている
